# AGENTS.md

> 当前需求更新：2026-09-19，依据 PRD v2.3 和 D072。旧版实现及历史验收保留；新版差异与待办见 `docs/任务清单.md` T018–T023。本次需求统一不改变运行代码；远程同步结果见 `docs/远程同步记录.md`。

适用范围：本文件夹内的“源素库 / 虚拟素材网站 / AI 数字素材交易平台”项目。

## 1. 项目定位

源素库是面向中国大陆 AI 视频、漫剧、游戏、短剧广告及虚拟内容制作方的数字素材认证与交易平台，以真人形象、漫剧形象为主，场景和物件/道具为辅。首版建设 Web 网站、微信小程序和管理后台，包含上传者会员制、权证链存证、审核上架、购买授权、下载交付和收益记录；下一版开发 App。

邀请码是成为会员的必要条件。价格与分成沿用旧默认配置并允许后台修改；授权期限待法律审阅。当前规则以 PRD v2.3 和 D072 为准，新增功能仍须开发验收。

## 2. Codex 每次开始任务前必须读取

按顺序读取：

1. `PROJECT_START.md`
2. `docs/README.md`
3. `docs/项目总控.md`
4. 与任务相关的 `docs/` 文件
5. `docs/决策记录.md`
6. 如果任务来自 GitHub Issue，读取对应 Issue 内容

如果文档之间出现冲突，以 `docs/决策记录.md` 的最新记录和 Robert 的最新明确指令为准；仍不清楚时先问 Robert。

## 3. 工作原则

- 用中文和 Robert 沟通，除非 Robert 用英文提问。
- 保持真实、客观、准确；无法确认的内容标记为“待确认”，不要编造。
- 一次只推进一个明确任务，不把无关优化混进来。
- 不擅自删除文件、重置代码、清空数据或泄露密钥。
- 不把 GitHub、Notion、腾讯云部署、腾讯云 COS、Vercel、微信支付、支付宝、认证接口等外部系统当作已接通；必须先确认当前状态。
- 若需求不合理、不准确或有逻辑问题，先指出并请 Robert 确认。

## 4. 任务执行流程

1. 复述本次任务目标和不做范围。
2. 读取相关资料。
3. 如资料不足，列出需要 Robert 确认的问题。
4. 修改文档或代码。
5. 运行必要检查；如果没有代码或无法运行，说明原因。
6. 汇报修改内容、验收方式、风险和建议下一步。
7. 如果任务状态、计划或重要决策发生变化，同步更新 `docs/项目总控.md`、`docs/任务清单.md`、相关 docs，以及已接入的 Notion 总控页或 GitHub Issue。

## 5. 完成后汇报格式

```text
已完成：

修改文件：

如何验收：

检查结果：

风险/待确认：

建议下一步：
```

## 6. 当前限制和外部系统状态

- 当前本地文件夹已连接 GitHub 仓库：`nn54ym5b2d-dot/AI-website`。
- GitHub Actions 已接入并通过基础 CI 检查。
- GitHub Pull Request 工作流已接入，主分支保护已启用；后续代码任务默认走分支 + PR + Actions。
- T009A 项目资料同步见 PR #22。
- Notion 总控页已创建，本地同步源为 `docs/项目总控.md`。
- GitHub Issues 已创建 T006-T016（#1-#11）、T017（#16）和 T009A（#21）。
- GitHub Project #1 已创建：`源素库 MVP 开发`，已加入 Issues #1-#11、#16 和 #21。
- 腾讯云部署预览只有在对应外部服务接通后才能正式使用。
- 原始产品 Word `虚拟素材网站产品文档_v1.docx` 的有效内容已拆分进 `docs/`；Robert 已于 2026-07-19 主动删除旧 Word，不需要恢复或继续维护。当前规则以 `docs/决策记录.md`、相关 `docs/` 和 Robert 最新明确指令为准。
