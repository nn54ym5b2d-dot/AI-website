# 全站占位功能审计

版本：v1.8
日期：2026-07-22
状态：审计完成；T012A-T014 已验收完成，T015 Review，T016 Blocked，T017 Backlog

## 1. 审计目标与判定标准

本次审计覆盖当前 `app/` 路由、页面可见文案、按钮与链接、对应 API/Prisma 数据来源，以及 T012A-T017 的任务范围。目的不是临时扩展已经完成的 T012，而是确保第一版已经露出的入口最终都有真实业务闭环，或明确保留为外部服务边界。

以下任一情况视为“占位未落地”：

- 页面使用硬编码演示记录、演示数量或演示金额，却看起来像真实业务数据。
- 按钮可点击但只改变本地组件状态，不保存数据、不调用 API，也没有明确“尚未开放”的不可操作状态。
- 页面只有流程说明或字段清单，没有真实查询、提交、权限和状态流转。
- 已有路由和导航入口，但对应的后续任务没有逐项写明数据、API、页面和验收责任。
- 页面文案宣称“精选”“认证可查”等能力，但实际数据只能证明更弱的含义。

明确标记为 `local_test`、非真实 seed 或“真实外部服务尚未接入”的实现不自动视为遗漏；只要本地业务闭环真实可验证，并已由 T017 明确负责替换生产 provider，即属于受控开发边界。

## 2. 审计结论

当前 T009-T014 已完成的身份、素材浏览、上传、审核认证、基础管理、交易、授权、ZIP 下载和收益主链路都使用真实本地 PostgreSQL/Prisma 与受控测试 provider，不是页面占位。全站当前仍有两类待完成项：

1. **T015 权限与观察员闭环**：`/admin/users` 的受约束角色/状态管理、观察员账号生命周期和 `/observer` 真实只读聚合已完成本地实现并进入 Review。
2. **T016 最终质量门槛**：仍需逐路由确认正式导航完全脱离通用 fallback，并清理无引用演示文件；生产 provider 和正式法律文本继续由 T017 负责。

本审计最初新增的 T012A 以及后续 T013-T014 均已验收合并；T015 已进入 Review，T016 等待 T015 验收合并而保持 Blocked，T017 保持 Backlog。

## 3. 逐项清单与任务归属

| 位置/入口 | 当前真实状态 | 审计前是否有明确后续要求 | 处理任务 | 必须落地的结果 |
|---|---|---|---|---|
| `/admin/invitations` | T012A 已接入真实列表、创建、掩码、逐条眼睛查看和禁用；查看/写操作记录审计 | 否 | T012A | 新邀请码加密保存并可按需查看；旧哈希历史码不可恢复；已使用邀请码不能禁用或复活 |
| `/admin/settings` | T012A 已接入 `system_settings` migration/seed、读取/修改 API 和表单 | 否 | T012A | 已验收；仅超级管理员可写，运营/财务只读 |
| `/admin/users` | T015 已增加受约束角色/状态管理、最后一名超级管理员保护和禁用会话撤销 | 已补齐，待验收 | T012A + T015 | 真实 PostgreSQL/API；仅超级管理员可写 |
| `/account` 购买者中心 | 使用真实购买和可下载计数，只展示购买、下载和授权；有效上传者可切换到独立上传者中心 | 否 | T012A、T013、T014 | 已接入 PostgreSQL 聚合，不混排上传业务入口 |
| `/account/uploader` 上传者中心 | 使用真实上传和收益聚合，只展示上传、审核状态、收益和上传者资料 | 否 | T014 | 仅有效上传者可进入；双角色账号可返回购买者中心 |
| `/account/uploads` | T012A 已接入真实上传列表、状态、时间和编辑/结果入口 | 否；T011 已有可复用上传者素材 API | T012A | 已验收并通过 PR #35 合并 |
| `/account/upload-status` | T012A 已接入真实审核、认证、退款待办和上架状态 | 否 | T012A | 已验收并通过 PR #35 合并 |
| `/account/uploader-profile` | T012A 已接入资料 GET/PATCH、校验、权限和真实表单 | 否 | T012A | 已验收并通过 PR #35 合并 |
| 素材详情“认证状态可查” | T012A 已只公开认证状态、证书编号、来源和签发日期 | 否 | T012A | 私有证书文件、凭证、证明材料和内部备注未进入公开响应 |
| 首页“本周精选素材” | T012A 已按实际 `newest` 语义改为“最新上架素材” | 否 | T012A | 未来精选必须新增真实字段与运营规则 |
| 条款/隐私/商业授权说明 | T012A 已增加三个版本化页面，当前文本明确标记本地草案/测试 | 不充分 | T012A + T017 | T017 换入正式审阅文本并验证接受版本 |
| 素材详情“加入待购清单” | T013 已接入可恢复的服务端订单草稿，校验身份、素材可售性、重复项和服务端价格 | 原 T013 范围不充分 | T013 | 已验收并通过 PR #37 合并 |
| `/checkout` | T013 已接入真实本地订单明细、服务端计价、测试支付 provider 和回调状态 | 原方向有，页面范围不具体 | T013 | 已验收并通过 PR #37 合并 |
| `/account/purchases` | T013 已接入当前用户真实订单/购买明细和支付状态 | 原要求不充分 | T013 | 已验收并通过 PR #37 合并 |
| `/account/licenses` | T013 已接入支付成功后的真实授权、授权快照和下载资格 | 原要求不充分 | T013 | 已验收并通过 PR #37 合并 |
| 认证上传费支付 | T013 已接入待支付结算；验证成功后原子进入 `pending_review` | 原页面/结果要求不完整 | T013 | 已验收并通过 PR #37 合并 |
| 认证费退款 | T013 已接入真实退款待办、处理结果、幂等回调和审计 | 原后台入口不明确 | T013 | 已验收并通过 PR #37 合并 |
| `/admin/orders` | T013 已接入真实订单/明细查询、状态筛选和详情 | 原要求不充分 | T013 | 已验收并通过 PR #37 合并 |
| `/admin/payments` | T013 已接入真实支付与退款记录，并按后台角色裁剪字段和操作 | 原要求不充分 | T013 | 已验收并通过 PR #37 合并 |
| `/admin/licenses` | T013 已接入真实授权查询、订单关联和授权版本快照 | 原要求不充分 | T013 | 已验收并通过 PR #37 合并 |
| `/account/downloads` | T014 已接入授权、资格、真实 ZIP 状态、短时地址和下载历史；首次点击在按需生成完成后自动下载 | 已完成 | T014 | PR #39 已验收合并；不公开内部存储定位，不向用户暴露两步“准备 ZIP”流程 |
| `/account/revenue` | T014 已接入有效上传者的总购买次数、各素材购买次数和实际购买收益 | 不充分 | T014 | 退款/撤销不计入有效购买；不显示平台、他人收益或内部结算状态 |
| `/admin/revenue` | T014 已接入真实收益与冲正，并按后台角色裁剪字段 | 已完成 | T014 | PR #39 已验收合并 |
| 搜索“热门”排序 | T014 已按真实下载次数降序并稳定排序 | 有历史说明，未指定完成任务 | T014 | 本地实现完成，不再回退最新冒充热门 |
| `/admin/observer-accounts` | 真实账号列表、创建、启用/禁用/撤销和审计 | 已补齐，待验收 | T015 | 仅超级管理员可操作；撤销后不可重新启用 |
| `/observer` | 日/周/月/自定义真实聚合、分类型汇总和本人 0% 分成 | 已补齐，待验收 | T015 | 严格只读、最大一年、不可导出或下钻 |
| `/admin` 完整经营指标 | 已按角色接入订单、支付、授权、下载、收益、用户与观察员真实汇总 | 已补齐，待验收 | T013-T015 | 无权限的数据不查询、不展示 |
| `app/account/[section]`、`app/admin/[section]` | T012A-T014 已让对应正式路由使用专用页面并让未知 section 返回 404；仍保留少量待 T015 的准确不可用 fallback | 否 | T016 | T015 后确认全部 P0/P1 路由由真实专用页面接管，并移除正式导航对 fallback 的剩余依赖 |
| 废弃演示文件 | `lib/domain/demo-content.ts`、`components/upload/upload-prototype-form.tsx` 已无正式页面引用 | 否 | T016 | 确认无引用后删除或隔离出正式构建，避免后续误接回生产页面 |
| 真实短信/邮件/微信、COS、图片处理、CDN、支付、政府认证跳转 | 当前明确使用本地 provider 或人工流程 | 是 | T017 | 替换生产 provider、完成密钥/回调/权限/失败处理和上线核验；本地 `local_test` 标识与非真实数据不得进入生产 |

## 4. T012A 历史范围与完成边界

任务名称：**T012A 后台基础管理与现有入口补全**。

前置 T012 与后置 T013 均已完成；本节保留 T012A 当时的范围和责任边界，不代表当前仍在等待 T013。

T012A 必须完成：

- 邀请码管理真实闭环。
- 系统设置真实闭环，并为 T013/T014 提供服务端价格、认证费、分成比例和下载资格配置来源。
- 上传者“我的上传 / 审核状态 / 上传者资料”和个人中心已落地数据的真实化。
- 管理员用户列表/详情与角色摘要的只读基础；敏感角色修改留给 T015。
- 素材公开认证安全摘要。
- 首页“本周精选”语义修正。
- 版本化条款、隐私和商业授权展示入口；当前非正式文本必须明确标记。
- 对当时仍等待 T013-T015 的正式路由显示准确的“尚未开放/对应任务”状态，移除会误导用户的假记录与无行为按钮；T013-T014 后续已改由真实专用页面接管。

T012A 不做：

- 不开发订单支付、下载 ZIP、收益结算或观察员汇总。
- 不接入真实短信、邮件、微信、COS、CDN、支付商户或政府认证网站。
- 不在 T012A 开放管理员角色变更、观察员账号生命周期等 T015 高权限写操作。

## 5. 系统设置生效规则

- 设置值由服务端读取，前端提交的价格、费率和资格天数不能作为权威值。
- 新订单、新认证费和新授权按创建时的有效设置生成不可变快照；后续修改设置不得重写历史订单、授权、收益或退款金额。
- 已上架但尚未进入订单的素材价格使用当前有效设置；订单创建后固定 `order_items` 价格快照。
- 上传者 80%、平台 20%、观察员 0% 是首版默认 seed，不是散落在页面中的永久硬编码。
- 设置修改必须校验范围、记录操作者和前后值；只有超级管理员可写。

## 6. MVP 无占位完成门槛

T016 完成前必须进行一次从导航入口出发的逐路由复核：

- 每个第一版 P0/P1 入口要么有真实本地数据库/API/权限闭环，要么明确属于 T017 的生产 provider 替换边界。
- 正式导航可达页面不得包含伪造的订单、金额、收益、用户或素材记录。
- 可点击控件必须执行实际行为；尚未开放的行为使用不可点击状态和明确说明。
- 未知动态 section 返回 404，不得落入通用假数据页面。
- 桌面与 390px 页面无横向溢出、主要操作可完成、控制台无错误；交易与权限关键路径还需自动化测试。
