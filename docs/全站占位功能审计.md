# 全站占位功能审计

版本：v1.2
日期：2026-07-20
状态：审计完成；T012A 整改已验收合并并转为 Done，T013-T017 责任已写入规划

## 1. 审计目标与判定标准

本次审计覆盖当前 `app/` 路由、页面可见文案、按钮与链接、对应 API/Prisma 数据来源，以及 T012A-T017 的任务范围。目的不是临时扩展已经完成的 T012，而是确保第一版已经露出的入口最终都有真实业务闭环，或明确保留为外部服务边界。

以下任一情况视为“占位未落地”：

- 页面使用硬编码演示记录、演示数量或演示金额，却看起来像真实业务数据。
- 按钮可点击但只改变本地组件状态，不保存数据、不调用 API，也没有明确“尚未开放”的不可操作状态。
- 页面只有流程说明或字段清单，没有真实查询、提交、权限和状态流转。
- 已有路由和导航入口，但对应的后续任务没有逐项写明数据、API、页面和验收责任。
- 页面文案宣称“精选”“认证可查”等能力，但实际数据只能证明更弱的含义。

明确标记为 `local_test`、非真实 seed 或“真实外部服务尚未接入”的实现不自动视为遗漏；只要本地业务闭环真实可验证，并已由 T017 明确负责替换生产 provider，即属于受控开发边界。

## 2. 审计结论

当前 T009-T012 已完成的身份、素材浏览、上传、审核与认证记录主链路是真实 PostgreSQL/Prisma 业务实现，不是页面占位。但全站仍有三类缺口：

1. **原规划没有明确接手的现有入口**：邀请码管理、系统设置、上传者个人页、公开认证摘要、法律文档展示和首页“本周精选”语义。
2. **后续任务方向正确但范围写得过粗**：结算页、购买/下载/授权/收益个人页，以及订单、支付、授权、收益、用户和观察员后台。
3. **最终质量门槛缺失**：没有明确要求在 MVP 收口时删除通用演示页、无行为按钮和废弃原型组件。

因此新增 T012A 作为 T012 与 T013 之间的基础管理和现有入口补全任务；T013 从 Ready 调整为 Blocked，待 T012A 完成后开始。T014-T017 补齐逐路由责任，T016 增加“全站无伪功能占位”总验收门槛。

## 3. 逐项清单与任务归属

| 位置/入口 | 当前真实状态 | 审计前是否有明确后续要求 | 处理任务 | 必须落地的结果 |
|---|---|---|---|---|
| `/admin/invitations` | T012A 已接入真实列表、创建、掩码、逐条眼睛查看和禁用；查看/写操作记录审计 | 否 | T012A | 新邀请码加密保存并可按需查看；旧哈希历史码不可恢复；已使用邀请码不能禁用或复活 |
| `/admin/settings` | T012A 已接入 `system_settings` migration/seed、读取/修改 API 和表单 | 否 | T012A | 已验收；仅超级管理员可写，运营/财务只读 |
| `/admin/users` | T012A 已提供真实只读列表/详情、角色摘要和联系方式遮罩 | 不充分 | T012A + T015 | T015 再增加受约束角色/状态管理和最后一名超级管理员保护 |
| `/account` 数字卡片 | T012A 已改为真实已落地计数；交易与收益明确显示尚未开放 | 否 | T012A、T013、T014 | T013/T014 接入后补充交易与收益计数，禁止伪造数字 |
| `/account/uploads` | T012A 已接入真实上传列表、状态、时间和编辑/结果入口 | 否；T011 已有可复用上传者素材 API | T012A | 已验收并通过 PR #35 合并 |
| `/account/upload-status` | T012A 已接入真实审核、认证、退款待办和上架状态 | 否 | T012A | 已验收并通过 PR #35 合并 |
| `/account/uploader-profile` | T012A 已接入资料 GET/PATCH、校验、权限和真实表单 | 否 | T012A | 已验收并通过 PR #35 合并 |
| 素材详情“认证状态可查” | T012A 已只公开认证状态、证书编号、来源和签发日期 | 否 | T012A | 私有证书文件、凭证、证明材料和内部备注未进入公开响应 |
| 首页“本周精选素材” | T012A 已按实际 `newest` 语义改为“最新上架素材” | 否 | T012A | 未来精选必须新增真实字段与运营规则 |
| 条款/隐私/商业授权说明 | T012A 已增加三个版本化页面，当前文本明确标记本地草案/测试 | 不充分 | T012A + T017 | T017 换入正式审阅文本并验证接受版本 |
| 素材详情“加入待购清单” | 只修改当前组件内状态，刷新即丢失 | T013 只写订单，未明确待购状态 | T013 | 使用服务端订单草稿或等价可恢复方案；身份、素材可售性、重复项和服务端价格均受校验 |
| `/checkout` | 只有购买步骤说明，没有订单、金额、支付方式或状态 | 方向有，页面范围不具体 | T013 | 真实本地订单明细、服务端计价、微信/支付宝测试 provider、支付结果与失败/重复回调状态 |
| `/account/purchases` | 三条硬编码演示记录 | 不充分 | T013 | 当前用户真实订单/购买明细和支付状态，支持进入订单详情 |
| `/account/licenses` | 三条硬编码演示记录 | 不充分 | T013 | 支付成功后真实授权记录、授权快照和可阅读的授权版本 |
| 认证上传费支付 | 上传提交停在待支付，尚无支付入口 | 已归 T013，但页面/结果要求不完整 | T013 | 待支付认证费结算、测试支付成功后原子进入 `pending_review`、失败/重复回调安全处理 |
| 认证费退款 | T012 只创建退款待办 | 已归 T013，但后台入口不明确 | T013 | `/admin/refunds` 或支付模块内真实退款待办、处理结果、幂等回调和审计，不把待办显示成退款成功 |
| `/admin/orders` | 通用演示列表 | 不充分 | T013 | 真实订单/明细查询、状态筛选、详情和角色权限 |
| `/admin/payments` | 通用演示列表 | 不充分 | T013 | 真实支付与退款记录；运营只读、财务按权限处理、敏感字段遮罩 |
| `/admin/licenses` | 通用演示列表 | 不充分 | T013 | 真实授权记录查询、订单关联和授权版本快照 |
| `/account/downloads` | 三条硬编码演示记录 | 不充分 | T014 | 真实可下载授权、资格到期时间、ZIP 状态、短时下载链接和下载历史 |
| `/account/revenue` | 三条硬编码演示记录 | 不充分 | T014 | 上传者本人真实收益记录和订单明细关联，不显示平台或他人收益 |
| `/admin/revenue` | 通用演示列表 | 不充分 | T014 | 真实上传者 80%/平台 20% 收益记录、结算状态和权限过滤 |
| 搜索“热门”排序 | 明示没有热度并回退最新 | 有历史说明，未指定完成任务 | T014 | 基于真实已支付订单/下载统计实现稳定热门排序；若 MVP 不做则从选项中移除，不长期保留伪热门 |
| `/admin/observer-accounts` | 通用演示列表 | T015 方向有，未明确账号生命周期 | T015 | 观察员账号创建、绑定合作方、启用/禁用、权限撤销和审计；仅超级管理员可操作 |
| `/observer` | 只展示“可见/不可见字段”说明，没有任何经营数据 | T015 方向有，页面范围不具体 | T015 | 日/周/月/自定义区间真实汇总，自定义最大一年；严格只读、聚合、0% 分成字段且不可导出 |
| `/admin` 完整经营指标 | T012 只显示真实审核指标，没有订单、支付、收益或观察员指标 | 后续任务未逐项说明 | T013-T015 | 每个数据域落地后逐步接入真实指标；无权限的数据不查询、不展示，未实现时显示明确状态而非演示数字 |
| `app/account/[section]`、`app/admin/[section]` | 通用 fallback 向多个正式路由提供同一组假数据和无行为按钮 | 否 | T016 | 所有 P0/P1 路由由真实专用页面接管；未知 section 返回 404；不再以通用演示页承载正式导航入口 |
| 废弃演示文件 | `lib/domain/demo-content.ts`、`components/upload/upload-prototype-form.tsx` 已无正式页面引用 | 否 | T016 | 确认无引用后删除或隔离出正式构建，避免后续误接回生产页面 |
| 真实短信/邮件/微信、COS、图片处理、CDN、支付、政府认证跳转 | 当前明确使用本地 provider 或人工流程 | 是 | T017 | 替换生产 provider、完成密钥/回调/权限/失败处理和上线核验；本地 `local_test` 标识与非真实数据不得进入生产 |

## 4. T012A 范围

任务名称：**T012A 后台基础管理与现有入口补全**。

前置：T012 验收、PR 合并并转 Done（已满足）。后置：T013。

T012A 必须完成：

- 邀请码管理真实闭环。
- 系统设置真实闭环，并为 T013/T014 提供服务端价格、认证费、分成比例和下载资格配置来源。
- 上传者“我的上传 / 审核状态 / 上传者资料”和个人中心已落地数据的真实化。
- 管理员用户列表/详情与角色摘要的只读基础；敏感角色修改留给 T015。
- 素材公开认证安全摘要。
- 首页“本周精选”语义修正。
- 版本化条款、隐私和商业授权展示入口；当前非正式文本必须明确标记。
- 对仍等待 T013-T015 的正式路由显示准确的“尚未开放/对应任务”状态，移除会误导用户的假记录与无行为按钮。

T012A 不做：

- 不开发订单支付、下载 ZIP、收益结算或观察员汇总。
- 不接入真实短信、邮件、微信、COS、CDN、支付商户或政府认证网站。
- 不在 T012A 开放管理员角色变更、观察员账号生命周期等 T015 高权限写操作。

## 5. 系统设置生效规则

- 设置值由服务端读取，前端提交的价格、费率和资格天数不能作为权威值。
- 新订单、新认证费和新授权按创建时的有效设置生成不可变快照；后续修改设置不得重写历史订单、授权、收益或退款金额。
- 已上架但尚未进入订单的素材价格使用当前有效设置；订单创建后固定 `order_items` 价格快照。
- 上传者 80%、平台 20%、观察员 0% 是首版默认 seed，不是散落在页面中的永久硬编码。
- 设置修改必须校验范围、记录操作者和前后值；只有超级管理员可写。

## 6. MVP 无占位完成门槛

T016 完成前必须进行一次从导航入口出发的逐路由复核：

- 每个第一版 P0/P1 入口要么有真实本地数据库/API/权限闭环，要么明确属于 T017 的生产 provider 替换边界。
- 正式导航可达页面不得包含伪造的订单、金额、收益、用户或素材记录。
- 可点击控件必须执行实际行为；尚未开放的行为使用不可点击状态和明确说明。
- 未知动态 section 返回 404，不得落入通用假数据页面。
- 桌面与 390px 页面无横向溢出、主要操作可完成、控制台无错误；交易与权限关键路径还需自动化测试。
