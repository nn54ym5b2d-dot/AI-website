# 测试验收清单

版本：v3.1
日期：2026-07-19

## 1. 使用方式

每完成一个开发任务，至少检查对应功能是否满足本文件的验收点。若暂时没有代码，本文件作为后续开发的验收依据。

## 2. 产品资料验收

- `docs/PRD.md` 能准确描述产品定位、目标用户和 MVP 范围。
- `docs/业务规则.md` 覆盖上传、认证、审核、购买、授权、收益和状态规则。
- `docs/页面清单.md` 能指导前台、个人中心和后台页面搭建。
- `docs/角色权限.md` 能明确不同角色能做什么、不能做什么。
- `docs/决策记录.md` 区分已确认和待确认问题。

## 3. 上传者流程验收

- 上传者不能无邀请码激活上传权限。
- 上传者可以创建人物、物件/道具、场景素材。
- 上传页使用两套表单：人物素材一套；物件/道具和场景素材共用一套。
- 上传者上传原文件；系统在对象校验后通过受控处理任务生成独立水印预览图和缩略图。
- 人物素材需要上传必要证明材料；物件/道具和场景素材不强制要求必要证明材料。
- 提交素材时进入认证上传费支付流程。
- 初审未通过时可以记录退款。
- 初审通过后可以进入认证状态。
- 认证完成后，管理员可以录入证书编号、凭证并保存认证证书文件。
- 所有素材，包括人物/物件/场景，必须获得认证证书后才可上架。

## 4. 购买下载流程验收

- 用户可以浏览、搜索、筛选素材。
- 用户可以查看素材详情、价格、认证状态和预览信息。
- 素材详情页只展示带水印的预览图，不能通过页面或预览地址获得原文件。
- 公开预览图和缩略图是从私有原文件预先/异步生成的独立文件，不能只靠前端叠加水印；生成失败时素材不得回退展示原图或进入上架状态。
- 首版人物和场景素材统一显示 50 元，物件/道具素材统一显示 10 元。
- 未登录用户不能直接购买。
- 一个订单可以包含多个素材。
- 支付成功后，每个素材生成独立授权记录。
- 支付成功后，用户可以获取默认有效 365 天的下载入口；多角度原文件以一个 ZIP 素材包交付。
- ZIP 文件清单覆盖该素材全部有效原文件，且不能混入其他素材文件。
- 原始文件地址不能直接公开。
- 购买后下载的是由该素材全部有效原文件生成的私有 ZIP；应用服务校验授权后只签发 ZIP 的短时地址，不直接公开单个原文件。
- 下载行为有记录。

## 5. 授权流程验收

- 授权记录包含购买用户、素材编号、订单编号、授权说明、授权时间和认证状态。
- 用户可以在个人中心查看自己的授权记录。
- 后台可以查看授权记录。
- 授权永久有效，不自然到期；授权状态支持有效、已撤销。

## 6. 收益流程验收

- 支付成功后，上传者收益记录增加。
- 首版每笔素材购买按上传者 80%、平台 20%、外部观察员 0% 记录收益。
- 上传者只能查看自己的收益。
- 管理员可以查看全部收益记录。
- 财务管理员可以查看收益和支付记录。
- 财务管理员可以发起和处理退款，且操作进入日志。
- 素材购买退款只能选择完整订单明细；拆分单个明细金额时必须拒绝，退款成功只撤销被退款明细的授权和收益。
- 第一版不要求自动提现。

## 7. 后台权限验收

- 超级管理员拥有全部后台权限；购买和上传仍要求对应基础角色。
- 运营管理员不能修改角色和系统级设置。
- 财务管理员不能审核素材、修改素材内容、录入认证信息或修改价格。
- 外部观察员只能只读查看平台上传量、下载量、收益汇总和合作方分成相关数据。
- 外部观察员首版分成比例和金额显示为 0。
- 外部观察员不能导出任何后台数据。
- 外部观察员不能查看用户手机号、邮箱、证件信息、支付账户、完整支付流水号、素材原文件地址或下载原始链接。
- 外部观察员可按人物、物件/道具、场景查看上传、上架、认证和下载汇总，但不能下钻到素材明细。
- 外部观察员数据看板支持日、周、月统计周期和自定义起止区间；单次自定义区间最大跨度为一年，超过一年时服务端必须拒绝。
- 关键后台操作进入操作日志。

## 8. 安全与合规验收

- 原始素材文件不公开暴露。
- 用户下载入口默认有效 365 天；每次实际下载使用短时 COS 签名 URL。
- 手机号注册、邮箱加手机号注册和微信加手机号首次创建账号都必须保存所接受的有效条款版本；未接受时不能创建新用户。
- 手机号验证后可以直接注册；邮箱注册缺少邮箱或手机号任一验证时必须拒绝；首次微信登录缺少手机号验证时必须拒绝。
- 已验证邮箱、手机号和微信必须绑定到同一用户；若两种身份分别属于不同账号，系统不得自动合并或创建重复账号。
- 实名认证不属于 MVP；系统不得收集或伪装保存未定义的实名资料。
- 后台接口必须检查角色权限。
- 敏感信息不向外部观察员展示。
- 微信支付和支付宝回调需要验签。
- 不在代码中写入密钥、token、密码。

## 9. Robert 手动验收建议

每个阶段完成后，Robert 可以按以下方式验收：

1. 先看页面是否符合业务理解。
2. 再按上传者、购买用户、管理员、外部观察员分别走一遍流程。
3. 检查不该看到的信息是否被隐藏。
4. 检查不该操作的按钮是否不可用或不可见。
5. 检查订单、授权、收益记录是否正确产生。

## 10. T008 API 接口合同验收

- 核心业务接口统一使用 `/api/v1` 前缀，成功和错误响应包含 `requestId`。
- 金额使用整数分，时间使用 ISO 8601 UTC，列表接口使用受限游标分页。
- Web 登录令牌只保存在 `HttpOnly`、`Secure`、`SameSite=Lax` Cookie，不写入 `localStorage`。
- Cookie 鉴权的修改请求同时校验来源和 CSRF token；未登录返回 `401`，角色或资源归属不足返回 `403`。
- 创建订单、发起支付、提交审核和退款接口要求 `Idempotency-Key`，重复请求不能生成重复业务记录。
- 支付/退款回调使用第三方事件标识去重，不依赖客户端 `Idempotency-Key`。
- 手机号/邮箱验证码、会话、上传意图、幂等和回调事件只保存必要哈希或摘要，不保存明文敏感值。
- 上传者先从应用服务申请短期签名地址，再直传私有 COS，最后调用完成接口由服务端核验对象。
- Next.js 应用服务器不接收素材文件正文；API 不单独返回 COS bucket、region 或 object key。
- Next.js 不读取、缓存、解码或处理图片正文；公开请求只返回已生成水印衍生图的受控地址，真实环境由 CDN 分发。
- 水印预览图/缩略图与原文件使用不同对象和权限，并记录来源文件、处理状态和水印模板版本；修改预览 URL 不能访问原文件。
- 支付和退款回调基于原始请求体验签，并校验商户号、订单号、金额、币种和重复事件。
- 素材购买支付成功后，订单、授权、365 天平台下载入口和收益记录在事务中幂等生成。
- 365 天指平台下载资格；实际 COS 签名 URL 每次下载时短时生成，建议不超过 10 分钟。
- 下载前重新校验用户、授权、入口、文件归属和有效期，且历史接口不返回 COS URL 或下载 token。
- 后台审核、上架、认证、退款、权限、设置和授权撤销均按服务端角色检查并记录操作日志。
- 人物证明和认证证书只允许超级/运营管理员按需获取短时查看地址，访问行为进入日志，财务和观察员无权查看。
- 外部观察员只读取汇总快照和自己的分成记录，不存在写接口或任何导出接口。
- 外部观察员响应不包含用户、单笔订单、支付流水、下载明细、COS、证明材料、证书文件或内部日志。
- 自动化检查包含真实单元测试，至少覆盖管理员/观察员边界、运营/财务权限差异和导航入口裁剪；`npm test` 不得只是类型检查别名。
- 真实 COS、支付、短信、邮件、微信和政府认证服务未接入时，只能使用 provider 占位或测试适配器，不能伪装为已接通。

## 11. 当前待确认

- 微信支付和支付宝已确认，但商户号和真实接口尚未接入，支付测试暂为流程级验收。
- 政府认证网站未确认，认证相关测试暂为人工录入流程。
- 技术方向和基础代码框架已确认；当前检查命令为 `npm run lint`、`npm run typecheck`、`npm test` 和 `npm run build`。

## 12. T009 身份基础验收

- T009 已按本节完成最终检查并通过 PR #19 合并：`npm run lint`、`npm run typecheck`、`npm test`（10/10）、`npm run build` 和 GitHub Actions 均通过。
- 全新环境可按 README 启动 PostgreSQL 16、应用 migration、重复载入非真实 seed 并运行身份 API 测试。
- 注册在同一事务中创建用户、已验证登录身份、`buyer` 基础角色和当前有效条款接受记录。
- 会话 token、CSRF token 和验证码在数据库中只保存哈希；会话 Cookie 包含 `HttpOnly`、`Secure`、`SameSite=Lax`。
- 缺少或错误 CSRF/Origin 的 Cookie 鉴权修改请求必须拒绝。
- 邀请码只能原子激活一次，并同时创建上传者资料和有效 `uploader` 角色。
- 重复运行本地 seed 不得复活已使用、禁用或过期的邀请码；已关联上传者的历史不一致邀请码必须恢复为已使用，新测试邀请码独立创建且不删除旧资料。
- `admin` 基础角色和有效后台子角色必须同时存在；超级管理员不自动获得 `buyer` 或 `uploader`。
- `observer` 不能进入管理后台，且只返回自己的合作方摘要，不返回管理员权限。
- `npm test` 只允许重置数据库名以 `_test` 结尾的隔离测试库。

## 13. T009A 视觉设计基线验收

- 2026-07-18 最终复核：本地 Docker Compose PostgreSQL 16 为 `healthy`；`npm test` 对隔离的 `yuansu_test` 完成 reset、migration、幂等 seed，覆盖统一手机号/邮箱登录注册、邮箱/微信绑定、条款、验证码不可重放、昵称生成、安全回跳、退出登录、邀请码激活和已用邀请码 seed 保持，14/14 通过；lint、typecheck、build 通过。817×735 桌面、390×844 手机、真实本地邮箱登录、个人中心安全退出、旧会话失效、登录态首页头部及购买用户上传者激活入口均通过；PR #24 GitHub Actions `verify` 成功并已合并。
- 已使用相同页面内容生成三套差异明确的视觉方向，未在选择方向前修改正式页面样式。
- Robert 已选择一套明确的主方向；如借用其他方向元素，调整范围有明确记录。
- `docs/视觉设计基线.md` 已补齐颜色、字体、字号、间距、圆角、边框、阴影、图片比例、图标、组件和常见状态规范。
- 首页、素材浏览/搜索、素材详情、登录/注册、个人中心/上传和管理后台六组代表页面使用统一视觉。
- 至少检查约 1440px 桌面端和约 390px 手机端，不出现非预期横向滚动、关键按钮遮挡、文字溢出或图片拉伸。
- 素材图片是前台页面视觉重点，认证状态、价格和购买入口层级清楚，管理后台和观察员入口不在公开页面突出展示。
- 核心导航、搜索筛选、标签切换、登录注册表单和主要按钮可操作；演示素材与数据明确标记为非真实。
- 未登录桌面头部显示“登录/注册”和“上传素材”，不显示两个分离认证按钮或独立放大镜；手机菜单保留文字搜索入口并使用相同的两个动作。
- “登录/注册”进入 `/login?next=%2F`，认证成功回首页；“上传素材”进入 `/login?next=%2Fupload`，认证成功进入 `/upload`。无效或站外 `next` 必须回退首页。
- 登录后首页头部用用户图标和昵称替换“登录/注册”，并可进入个人中心；“上传素材”直接进入 `/upload`。
- 个人中心清楚展示“退出登录”；点击后必须由服务端撤销当前会话、清除会话 Cookie 并返回首页，首页恢复未登录头部，旧会话不能继续访问个人中心。
- 非上传者进入 `/upload` 时显示邀请码激活界面而不是无权限页；激活后留在 `/upload` 并展示提交界面；已有上传者直接展示提交界面。
- 手机号入口标签显示“手机号登录”，微信入口显示“微信登录”；国家/地区代码下拉默认 `+86 中国大陆`，切换其他区号后仍能向原身份 API 提交有效的完整号码。
- 登录/注册页顶部直接展示手机号和微信并默认选中手机号，不显示“主要方式”标题；邮箱位于表单下方“其他方式”独立区域；QQ 不展示。
- 邮箱注册界面明确分为邮箱验证和手机号绑定两步；微信首次使用提示必须绑定手机号；未接入真实微信时页面明确标记 T017 待接入，不展示伪造的可扫描二维码。
- 注册表单不显示“显示名称”；手机号/邮箱新账号由服务端生成随机昵称，微信新账号优先使用清理后的授权昵称，绑定已有账号不得覆盖原昵称。
- 同一手机号、邮箱或微信身份不得重复建号；已有身份通过统一入口返回登录结果，新身份才要求条款并创建；成功消费的验证码不得重放，站外 `next` 地址不得被用于跳转。
- 手机号状态的条款提示和“登录/注册”主按钮位于“其他方式”分隔线之前；邮箱状态的条款与主按钮位于邮箱字段之后。
- 页脚和正文不重复显示独立注册入口；旧 `/register` 安全转向统一 `/login` 并保留合法站内回跳目标。
- 键盘焦点可见，表单输入有标签，错误不只依赖颜色表达，手机端主要点击区域清楚可用。
- T006 路由和角色入口未被擅自改变；T009 会话、CSRF、邀请码和角色守卫未被视觉改动削弱。
- 未提前接入 T010-T017 的素材数据库、支付、COS、上传、审核、下载、认证或生产服务。
- 同视口对比选中视觉稿与页面实现，修正明显的布局、裁切、留白、字体、颜色、边框和圆角差异。
- `npm run lint`、`npm run typecheck`、`npm test`、`npm run build` 和 GitHub Actions `verify` 均通过。

## 14. T010 素材浏览和详情页验收

- 本地 PostgreSQL 16 已应用 T010 migration，并可重复执行 seed；人物、物件/道具、场景三类非真实素材可经公开 API 查询。
- 公开列表只返回 `listed + approved + certified` 且至少存在一个处理成功的 `public_preview` 水印衍生图的素材；未上架、未认证、处理失败或软删除素材不公开。
- `/api/v1/assets` 支持关键词、类型、标签、价格、上架时间、排序和游标分页；详情、分类和标签接口使用同一真实 PostgreSQL 数据源。
- 公开响应不包含私有原文件的 bucket、region、object key、来源文件 ID、证明材料或下载地址；公开水印衍生图与私有原文件使用独立文件记录和不透明对象路径。
- 首页、分类、搜索筛选和素材详情页不再直读 T009A mock；详情支持多角度带水印预览、认证状态、价格、标签、永久商业授权摘要和购买入口。
- 公开图像是已在文件中固化水印的本地衍生图，Next.js 请求不读取、解码或加工原图；真实 COS、图片处理和 CDN 仍留到 T017。
- 已完成桌面端和 390×844 手机端浏览器检查，未发现非预期横向滚动、文字溢出、图片拉伸或关键操作遮挡。
- 本地数据库集成测试当前 17/17；`npm run lint`、`npm run typecheck`、`npm test` 和 `npm run build` 均通过。PR #28 GitHub Actions `verify` 成功，Robert 完成网页预览验收后已压缩合并到 `main`（`5072265`）。
