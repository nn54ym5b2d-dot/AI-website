# 业务规则

## 当前需求与实施状态（2026-09-19）

- 当前产品基线：`docs/PRD.md` v2.3；Word：`output/doc/项目PRD_2026-09-19/数字资产认证项目PRD_v2.3.docx`；决策 D072；以人像（真人形象、漫剧形象）为主，场景和物件/道具为辅。首版为 Web 网站＋微信小程序＋管理后台，包含上传者会员制；App 下一版。
- 默认配置继续沿用：认证上传费每份 10 元；人像（真人、漫剧）及场景每份 50 元，物件/道具每份 10 元；上传者/平台/观察员分成 80%/20%/0%。后台可修改，只影响后续新业务，不重算历史快照。
- 有效邀请码是成为上传者会员的必要条件，不等于全部入会条件；会员等级、费用、权益、到期及上传资格联动规则另行制定。购买者身份保留。
- 授权期限属于法律问题，待法律审阅确认；不得统一承诺永久授权。下载资格默认 365 天，与授权期限及单次短时下载地址有效期分别管理。
- 权证链方案已确认：平台审核后提交文件哈希，取得并核验存证编号、证书及凭证，绑定素材版本后上架；生产接入尚未完成。
- 本轮更新文档与待办并按授权远程同步，不修改业务代码或数据库；同步结果见 `docs/远程同步记录.md`。T016 Ready（既有 Web 回归）、T017 Backlog；新增 T018–T023（Issues #45–#50）见任务清单。旧开发/验收记录只证明当时范围，新版功能不得据此标为完成。


版本：v2.2（2026-09-19 需求统一修订）
日期：2026-09-19

## 1. 上传者规则

- 有效邀请码是成为上传者会员的必要条件；注册账号不等于已成为会员，其他入会条件及会员细则另行制定。
- 公开业务账号注册后默认是购买者；开通上传资格后必须同时持有 `buyer + uploader`，上传资格不替换购买者身份。
- 上传者继续拥有购买、订单、授权和下载能力，同一账号不需要注册第二套购买账号。
- `/account` 购买者中心和 `/account/uploader` 上传者中心分别展示各自业务入口，但共用同一账号与登录状态。
- 同一个上传者账号可以上传人像（真人形象、漫剧形象）、物件/道具和场景素材。
- 第一版不开放无邀请码入会；会员与上传资格的具体联动由 T018 明确，不推定邀请码已替代会员系统。
- 邀请码只能激活一个上传者账号；一旦已使用或已关联上传者资料，不得通过后台操作、测试 seed 或状态修复重新开放。
- 企业用户第一版可先合并为购买用户；企业账户、企业授权、批量采购和对公支付放到后续版本。

## 2. 素材上传规则

- 第一版处理图片、多角度照片、漫剧角色立绘、多视图、表情及设定图，以人像为主，场景和道具为辅。
- 每份素材需要保存全部角度原文件、由系统异步生成的独立水印预览图/缩略图、文件信息和哈希值；公开页面和 CDN 只访问水印衍生图，购买后把全部有效原文件打包为一个私有 ZIP 交付。
- 上传者提交素材时需要填写素材名称和类型；说明可选填。
- 真人形象必须提交形象授权材料，漫剧形象必须提交创作或合法授权材料。
- 物件/道具和场景素材可以选填来源、版权或所有权证明；选填材料与人物证明一样保持私有，但不是提交前置条件。
- 所有素材，包括人物/物件/场景，均需获得认证证书后才能上架。
- 身份核验按权证链接入及业务要求落实，具体资料与保存范围在接入设计中确认，不收集未定义的实名资料。

## 3. 认证上传费规则

- 认证上传费默认每份素材 10 元。
- 10 元为后台可配置字段，后续可调整。
- 上传提交时支付认证上传费。
- 初审未通过时，当场创建认证费原路退款待办；退款成功只能由已验证的支付平台回调或主动查询确认，不能由页面或管理员直接写成成功。
- 初审通过后固定文件版本并通过权证链接口提交文件哈希，取得并核验存证凭证。
- 认证上传费长期独立记录在 `certification_fee_charges`，不并入素材购买订单。

## 4. 审核和认证规则

| 阶段 | 规则 |
|---|---|
| 初审 | 平台检查素材基础质量、类型、信息完整度和明显违规情况 |
| 权证链存证 | 初审通过后提交文件哈希，启用生成证书及上链；权证链对接北京互联网法院天平链 |
| 认证记录 | 查询并核验存证结果，保存证据编号、链上编号、证书及凭证，绑定素材文件版本；文件变更重新审核存证 |
| 上架 | 审核通过、存证成功且凭证完整，并有可用水印预览后才允许上架 |

既有实现记录（不代表新版接口完成）：T012 已把旧版人工认证规则落实为本地数据库状态机：审核通过创建人工认证记录；认证通过必须填写证书编号并关联私有证书文件；上架还要求存在可用水印预览。审核、认证、上架/下架、基础信息编辑和敏感文件查看均记录操作日志。真实认证费退款执行属于 T013，真实 COS 和权证链存证服务属于 T017。

当前待确认：

- 权证链生产账号、必要身份核验及凭证样式的联调验收。
- 认证异常后的重新提交规则。

## 5. 购买规则

- 第一版采用单次购买模式。
- 一个订单可以购买多个素材。
- 用户需登录后才能创建订单和支付。
- 支付成功后，订单内每个素材分别开放下载权限。
- 支付成功后，订单内每个素材分别生成授权记录。

## 6. 定价规则

- 平台统一定价，不由上传者自由定价。
- 默认人像（真人形象、漫剧形象）每份 50 元、场景每份 50 元、物件/道具每份 10 元，均可后台修改。
- 后台系统设置保存素材分类价格，只有超级管理员可修改；运营和财务只读。
- 新订单的订单明细在创建时保存服务端当前价格快照，修改系统设置不得重写历史订单、授权、收益或退款金额。
- 新认证费记录同样保存创建时系统设置快照；T012A 已将上传草稿默认价格和认证费改为从服务端设置读取，客户端价格不作为权威值。
- 邀请码列表默认掩码，授权管理员可逐条按需查看完整码；查看必须通过权限校验并写入审计；只有未使用且未过期的邀请码可禁用，创建和禁用都必须写审计日志。
- 当前服务条款、隐私政策和商业授权为明确标记的本地草案/测试版本，不代表正式法律意见；T017 换入正式审阅文本并保留版本历史。
- 上传者不能自行定价。

## 7. 授权规则

用户付费购买后，只能在具体素材约定的用途和范围内用于商业内容制作；真人形象须明确 AI 合成等使用范围。

授权记录至少包括：

- 购买用户
- 素材编号
- 订单编号
- 授权条款版本、文本快照、具体用途和 AI 使用限制
- 授权期限（法律待定，正式条款确认后保存成交时快照）
- 授权时间
- 素材认证状态
- 必要使用限制

禁止行为至少包括：

- 转售素材原文件
- 冒充素材权利人
- 违法用途
- 侵权用途
- 平台规则明确不允许的其他用途

- 统一商业授权协议的正式法律文本。
- 是否区分个人商用和企业商用。
- 授权期限属于法律问题，待法律审阅确认，不预设永久或固定期限；退款、违规或争议处理可撤销并记录原因。

## 8. 下载规则

- 支付成功后按当时系统设置生成默认有效 365 天的用户下载资格快照，后续改设置不影响已有授权。
- 一份多角度素材以一个 ZIP 包下载，ZIP 包含该素材的全部有效原文件。
- 原始文件地址不能直接公开。
- 下载记录需要保存用户、素材、订单、下载时间和下载状态。
- 每次通过有效下载入口取文件时，由服务端生成短时 COS 签名 URL，不保存或公开长期原文件地址。

平台下载资格（默认 365 天）与每次签发的 COS 临时 URL（建议 10 分钟）是两个不同期限，不得共用同一个设置或把 365 天长效地址直接暴露给用户。

## 9. 收益规则

- 素材购买分成默认上传者 80%、平台 20%、观察员 0%，后台可修改，历史成交快照不重算。
- 支付成功后，上传者收益记录增加。
- 第一版只记录收益，不做自动提现和复杂分账结算。
- 提现/结算状态放到后续版本扩展。
- 退款后需要冲正对应的上传者和平台收益记录。
- 素材购买退款可以只退订单中的部分明细，但每个被选中的订单明细必须整项退款，不允许拆分单个明细金额。
- 观察员看板“交易收益”按本周期全部素材交易支付减交易退回计算，使用素材成交金额的 100% 口径；其中包含上传者分成和平台分成，不代表平台最终利润。
- 观察员看板“上传收益”按本周期成功支付的认证上传费减成功退回的认证上传费计算；待支付、支付失败和退款待办不计入。
- 观察员分类型“交易收益”按订单明细的素材类型快照汇总，同样使用 100% 成交净额口径，不下钻到单笔交易。

## 10. 状态规则

| 状态类型 | 建议状态 |
|---|---|
| 素材审核状态 | `draft`、`pending_review`、`approved`、`rejected` |
| 素材上架状态 | `unlisted`、`listed`、`delisted` |
| 认证状态 | `not_started`、`pending_payment`、`pending_review`、`certifying`、`certified`、`exception` |
| 订单状态 | `pending_payment`、`paid`、`cancelled`、`payment_failed`、`refunded`、`partial_refunded` |
| 授权状态 | `active`、`revoked` |
| 下载入口状态 | `active`、`expired`、`revoked` |
| 收益状态 | `recorded`、`reversed`、`settled_pending`、`settled` |
