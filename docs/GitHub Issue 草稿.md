# GitHub Issue 草稿

版本：v1.0
日期：2026-06-30
状态：草稿，待 Robert 审核；尚未创建到 GitHub Issues

## 使用说明

本文件用于先集中审核 T006-T016 的 GitHub Issue 内容。Robert 确认后，再按本文逐条创建到 GitHub Issues，并接入 GitHub Projects 看板。

每个任务默认规则：

- 每次只执行一个 Issue。
- 执行前必须读取 `PROJECT_START.md`、`AGENTS.md`、`docs/项目总控.md` 和相关 docs。
- 如任务中发现文档与需求冲突，以 `docs/决策记录.md` 和 Robert 最新明确指令为准。
- 完成后需要运行必要检查，并同步更新 Notion 总控页、`docs/项目总控.md`、`docs/任务清单.md` 和对应 Issue。

---

## [T006] 设计第一版页面结构和导航

任务目标：
明确第一版 Web 网站、上传者中心、个人中心、管理后台和外部观察员后台的页面结构、路由、导航关系和入口优先级。

相关资料：
- `docs/PRD.md`
- `docs/页面清单.md`
- `docs/角色权限.md`
- `docs/业务规则.md`
- `app/`
- `components/layout/site-header.tsx`

关联任务编号：
T006

本次范围：
- 梳理第一版页面分组、路由命名、导航层级和角色入口。
- 明确前台、上传者、购买用户、管理员、外部观察员分别从哪里进入。
- 更新 `docs/页面清单.md`，必要时同步 `docs/项目总控.md`。
- 在代码中建立结构优先的页面骨架和导航雏形，重点是路由和信息结构，不追求最终视觉精修。

不做范围：
- 不做最终品牌视觉设计。
- 不开发真实登录、支付、上传、订单、下载功能。
- 不接入数据库、COS、支付或认证外部服务。

影响范围：
- `docs/页面清单.md`
- `docs/项目总控.md`
- `app/`
- `components/`

验收标准：
- Robert 能看懂第一版网站有哪些页面、每类用户从哪里进入。
- 页面结构覆盖前台浏览、上传者、购买用户、管理后台、外部观察员只读看板。
- T007、T008 能基于页面结构继续细化数据库和 API。
- 当前页面骨架在桌面和手机宽度下不出现明显布局错乱。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 如有代码改动，运行 `npm run build`

备注：
T006 是结构和导航优先，不是最终 UI 定稿。

---

## [T007] 设计数据库表结构细化版

任务目标：
把当前数据库草案细化为第一版可开发的数据模型，明确核心表、字段、关系、状态枚举、索引和权限敏感字段。

相关资料：
- `docs/数据库设计.md`
- `docs/业务规则.md`
- `docs/角色权限.md`
- `docs/API.md`
- `docs/页面清单.md`

关联任务编号：
T007

本次范围：
- 细化用户、登录身份、上传者、邀请码、素材、素材文件、认证、订单、支付、退款、授权、下载、收益、后台角色、操作日志、外部观察员汇总等数据结构。
- 明确文件存储只保存 COS key/path，不保存公开原始文件地址。
- 明确人物证明材料和认证证书文件的保存字段。
- 明确外部观察员可见汇总数据和不可见敏感数据。
- 输出数据库实现建议，包括是否需要 Prisma、Drizzle 或 SQL migration 方案。

不做范围：
- 不连接真实数据库。
- 不创建生产数据表。
- 不写完整业务接口。

影响范围：
- `docs/数据库设计.md`
- `docs/技术方案.md`
- `docs/API.md`
- 后续数据库实现任务

验收标准：
- 每个核心业务流程都能在数据模型中找到对应记录。
- 授权、下载、收益、认证证书和外部观察员数据不缺关键字段。
- 明确哪些字段需要脱敏、哪些字段不能给外部观察员看。
- 明确后续实现数据库迁移时的工具建议和待确认点。

测试要求：
- 文档任务可不运行代码检查。
- 若改动类型文件或代码占位，运行 `npm run typecheck`。

备注：
如果需要新增技术选择，比如 ORM，请列出建议并等待 Robert 确认。

---

## [T008] 设计核心 API 细化版

任务目标：
把 API 草案细化为第一版可开发的接口说明，明确接口路径、方法、权限、请求参数、返回结构、错误状态和安全要求。

相关资料：
- `docs/API.md`
- `docs/数据库设计.md`
- `docs/业务规则.md`
- `docs/角色权限.md`
- `docs/测试验收清单.md`

关联任务编号：
T008

本次范围：
- 细化用户登录、邀请码激活、素材浏览、素材上传、审核认证、订单支付、授权下载、收益记录、后台管理、外部观察员只读接口。
- 明确每个接口允许哪些角色访问。
- 明确支付回调、COS 限时下载、文件上传方式的接口边界。
- 明确通用错误码、鉴权失败、权限不足、数据不存在、状态不允许等返回规则。
- 更新 `docs/API.md`，必要时同步 `docs/测试验收清单.md`。

不做范围：
- 不实现全部 API。
- 不接入真实微信支付、支付宝、COS 或认证网站。
- 不输出正式 OpenAPI 文件，除非 Robert 另行确认。

影响范围：
- `docs/API.md`
- `docs/测试验收清单.md`
- 后续前后端开发任务

验收标准：
- 后续 T009-T015 能按接口说明开发。
- 每个核心流程至少有清晰的请求、返回、权限和错误处理说明。
- 外部观察员接口明确只返回汇总或脱敏字段，不提供导出接口。

测试要求：
- 文档任务可不运行代码检查。
- 如新增接口占位代码，运行 `npm run lint`、`npm run typecheck`。

备注：
API 细化完成后，再决定是否需要 Apifox。

---

## [T009] 开发用户注册登录和角色基础

任务目标：
建立第一版用户身份和角色基础，让系统具备购买用户、上传者、管理员、财务、外部观察员等角色判断能力。

相关资料：
- `docs/角色权限.md`
- `docs/API.md`
- `docs/数据库设计.md`
- `lib/auth/roles.ts`
- `types/domain.ts`

关联任务编号：
T009

本次范围：
- 实现基础角色类型、权限判断和页面访问守卫雏形。
- 建立注册、登录、退出、当前用户信息、邀请码激活上传者身份的前后端占位流程。
- 支持手机号、邮箱、微信登录的结构设计，但真实短信、邮箱验证码、微信 OAuth 可先占位。
- 给上传者、管理员、外部观察员准备不同入口和权限状态。

不做范围：
- 不接入真实短信服务。
- 不接入真实微信登录。
- 不实现复杂账号安全策略。
- 不做最终 UI 视觉精修。

影响范围：
- `app/`
- `lib/auth/`
- `types/`
- `docs/API.md`
- `docs/测试验收清单.md`

验收标准：
- 代码中能清楚区分购买用户、上传者、超级管理员、运营、财务、外部观察员。
- 页面或接口能根据角色显示/限制基础入口。
- 外部观察员不能获得编辑、审核、退款、导出等权限。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm run build`

备注：
本任务先做身份和权限基础，不做真实第三方登录。

---

## [T010] 开发素材浏览和详情页

任务目标：
开发第一版素材浏览、分类、搜索筛选和素材详情页骨架，让购买用户能理解素材列表和购买入口。

相关资料：
- `docs/页面清单.md`
- `docs/业务规则.md`
- `docs/API.md`
- `docs/数据库设计.md`

关联任务编号：
T010

本次范围：
- 开发首页素材入口、分类页、搜索结果页、素材详情页。
- 支持人物、物件/道具、场景素材的基础展示。
- 展示认证状态、价格、预览信息和购买入口。
- 使用 mock 数据或本地数据结构，等待后续真实数据库/API 接入。

不做范围：
- 不做真实支付。
- 不开放原始文件下载。
- 不接入真实搜索服务。
- 不做最终商业视觉定稿。

影响范围：
- `app/`
- `components/`
- `lib/domain/`
- `types/`

验收标准：
- 用户能浏览素材列表、按类型进入分类、查看素材详情。
- 素材详情页明确显示价格、认证状态和购买入口。
- 未登录或未购买用户不能看到原始下载链接。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm run build`

备注：
本任务以可用浏览结构为主，真实数据接入可后续迭代。

---

## [T011] 开发上传者素材提交流程

任务目标：
开发上传者提交人物、物件/道具、场景素材的第一版流程，包括素材基础信息、文件信息、证明材料和提交审核状态。

相关资料：
- `docs/业务规则.md`
- `docs/页面清单.md`
- `docs/API.md`
- `docs/数据库设计.md`
- `lib/storage/tencent-cos.ts`

关联任务编号：
T011

本次范围：
- 开发上传者素材创建/编辑/提交页面。
- 区分人物、物件/道具、场景素材表单规则。
- 人物素材要求必要证明材料；物件/场景不强制证明材料。
- 所有素材必须进入认证流程，获得认证证书后才能上架。
- 建立 COS 文件 key/path 生成规则和上传服务占位。

不做范围：
- 不接入真实 COS 上传，除非 Robert 已准备好 Bucket 和密钥。
- 不处理真实认证上传费支付。
- 不实现真实政府认证跳转。

影响范围：
- `app/`
- `components/`
- `lib/storage/`
- `types/`
- `docs/API.md`
- `docs/数据库设计.md`

验收标准：
- 上传者能看到三类素材的提交入口。
- 人物素材证明材料规则被明确执行或提示。
- 素材提交后进入待审核/认证相关状态。
- 原文件地址不被公开展示。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm run build`

备注：
腾讯云 COS 未接入前，先做结构和安全边界。

---

## [T012] 开发后台素材审核和认证记录

任务目标：
开发管理后台的素材审核、认证证书记录和上架控制基础能力。

相关资料：
- `docs/业务规则.md`
- `docs/角色权限.md`
- `docs/页面清单.md`
- `docs/API.md`
- `docs/数据库设计.md`

关联任务编号：
T012

本次范围：
- 开发后台素材审核列表和详情。
- 支持审核通过、审核驳回、上架、下架的状态操作雏形。
- 支持录入认证状态、证书编号、凭证和认证证书文件记录。
- 明确只有认证完成后素材才可上架。
- 记录关键后台操作日志的结构或占位。

不做范围：
- 不自动对接政府认证网站。
- 不做真实证书 OCR 或自动校验。
- 不允许外部观察员操作审核或认证。

影响范围：
- `app/admin/`
- `lib/auth/`
- `types/`
- `docs/API.md`
- `docs/测试验收清单.md`

验收标准：
- 运营/超级管理员能看到审核和认证入口。
- 财务管理员和外部观察员不能审核、上架、录入认证。
- 认证证书文件和证书编号字段在流程中有明确位置。
- 未认证素材不能进入已上架状态。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm run build`

备注：
政府认证网站和证书样式仍是待确认项，先按人工录入设计。

---

## [T013] 开发订单、支付占位流程和授权记录

任务目标：
开发多素材订单、支付占位流程和支付成功后生成授权记录的基础能力。

相关资料：
- `docs/业务规则.md`
- `docs/API.md`
- `docs/数据库设计.md`
- `docs/测试验收清单.md`

关联任务编号：
T013

本次范围：
- 建立订单创建、订单明细、支付状态、退款记录的前后端结构。
- 支持一个订单包含多个素材。
- 支付成功后为每个素材生成独立授权记录。
- 支付方式结构支持微信支付和支付宝，但真实接口先占位。
- 更新相关文档中的支付和授权边界。

不做范围：
- 不接入真实微信支付/支付宝商户接口。
- 不处理真实支付回调验签。
- 不实现复杂分账或自动提现。

影响范围：
- `app/`
- `lib/`
- `types/`
- `docs/API.md`
- `docs/数据库设计.md`

验收标准：
- 用户能从素材详情或订单入口进入支付占位流程。
- 一个订单可包含多个素材。
- 支付成功占位后能生成授权记录结构。
- 退款记录结构不破坏收益和授权逻辑。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm run build`

备注：
真实支付接入前，必须保持密钥不进入代码仓库。

---

## [T014] 开发下载和收益记录

任务目标：
开发购买后限时下载、下载记录和上传者收益记录的基础能力。

相关资料：
- `docs/业务规则.md`
- `docs/API.md`
- `docs/数据库设计.md`
- `docs/测试验收清单.md`
- `lib/storage/tencent-cos.ts`

关联任务编号：
T014

本次范围：
- 建立已购素材下载入口和下载记录。
- 建立限时下载链接生成服务的接口边界。
- 建立上传者收益记录和收益汇总页面/接口占位。
- 明确原始文件地址不能直接公开。
- 如下载链接有效期仍未确认，先以配置项形式保留默认值。

不做范围：
- 不公开 COS 原始文件地址。
- 不做真实自动提现。
- 不做复杂结算系统。

影响范围：
- `app/`
- `lib/storage/`
- `types/`
- `docs/API.md`
- `docs/数据库设计.md`

验收标准：
- 已购用户能看到可下载素材入口。
- 未购买用户不能获取下载链接。
- 下载动作有记录。
- 上传者收益记录能基于订单明细产生。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm run build`

备注：
下载链接有效期建议在本任务前或本任务中由 Robert 确认。

---

## [T015] 开发后台权限和外部观察员只读视图

任务目标：
完善后台权限控制，并开发外部观察员只读数据看板，确保合作方只能查看允许范围内的平台汇总和分成相关数据。

相关资料：
- `docs/角色权限.md`
- `docs/业务规则.md`
- `docs/API.md`
- `docs/数据库设计.md`
- `lib/auth/roles.ts`

关联任务编号：
T015

本次范围：
- 完善超级管理员、运营、财务、外部观察员的后台权限判断。
- 开发外部观察员只读数据看板。
- 展示平台上传量、下载量、订单金额、净收益、合作方分成基数、预计分成、已结算和待结算金额。
- 隐藏用户手机号、邮箱、证件信息、支付账户、完整支付流水号、原文件地址、下载原始链接和内部操作日志。
- 禁止外部观察员导出数据。

不做范围：
- 不允许外部观察员编辑、审核、退款、录入认证、修改价格或管理账号。
- 不开放合作方独立 API，除非 Robert 另行确认。
- 不做真实结算付款。

影响范围：
- `app/admin/`
- `lib/auth/`
- `types/`
- `docs/API.md`
- `docs/测试验收清单.md`

验收标准：
- 外部观察员只能访问只读看板。
- 外部观察员看不到敏感字段和内部日志。
- 外部观察员没有导出入口。
- 管理员、运营、财务权限边界符合 `docs/角色权限.md`。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm run build`

备注：
这是权限敏感任务，完成后需要重点 review。

---

## [T016] MVP 全流程测试和修正

任务目标：
对第一版 MVP 进行端到端检查，验证上传、审核、认证记录、购买、授权、下载、收益、后台权限和外部观察员只读视图是否能串起来。

相关资料：
- `docs/测试验收清单.md`
- `docs/PRD.md`
- `docs/业务规则.md`
- `docs/角色权限.md`
- `docs/API.md`
- `docs/数据库设计.md`

关联任务编号：
T016

本次范围：
- 按购买用户、上传者、超级管理员、运营、财务、外部观察员分别走核心流程。
- 修正阻塞 MVP 验收的明显问题。
- 检查敏感信息、权限边界、下载链接、授权记录、收益记录是否符合规则。
- 更新验收结果和剩余风险。

不做范围：
- 不加入新业务大功能。
- 不做 App、小程序、会员、自动提现、复杂分账。
- 不把待确认的法律协议、政府认证网站、真实支付接口强行假定为已完成。

影响范围：
- 全项目
- `docs/测试验收清单.md`
- `docs/项目总控.md`
- `docs/任务清单.md`

验收标准：
- MVP 核心流程能完整演示或明确标记哪些环节是占位。
- 所有 P0 权限边界通过检查。
- GitHub Actions 通过。
- Notion、项目总控、任务清单和 GitHub Issue 状态同步。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm run build`
- 补充必要的手动验收记录。

备注：
本任务完成后，项目进入上线准备和真实外部服务接入检查阶段。
