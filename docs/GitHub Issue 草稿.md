# GitHub Issue 草稿

版本：v2.1
日期：2026-07-15
状态：已由 Robert 审核确认，已创建到 GitHub Issues，并已加入 GitHub Project #1

## 使用说明

本文件保留 T006-T017 与 T009A 的 Issue 草稿内容和 GitHub Issue 映射，方便后续核对任务来源。

GitHub Project：<https://github.com/users/nn54ym5b2d-dot/projects/1>

## GitHub Issue 映射

| 任务 | GitHub Issue | 链接 |
|---|---|---|
| T006 | #1 | <https://github.com/nn54ym5b2d-dot/AI-website/issues/1> |
| T007 | #2（Done，PR #14） | <https://github.com/nn54ym5b2d-dot/AI-website/issues/2> |
| T008 | #3（Done，PR #15；状态同步 PR #17） | <https://github.com/nn54ym5b2d-dot/AI-website/issues/3> |
| T009 | #4（Done，PR #19） | <https://github.com/nn54ym5b2d-dot/AI-website/issues/4> |
| T009A | #21（Ready；Project 为 Todo；资料同步 PR #22） | <https://github.com/nn54ym5b2d-dot/AI-website/issues/21> |
| T010 | #5（Blocked，等待 T009A） | <https://github.com/nn54ym5b2d-dot/AI-website/issues/5> |
| T011 | #6 | <https://github.com/nn54ym5b2d-dot/AI-website/issues/6> |
| T012 | #7 | <https://github.com/nn54ym5b2d-dot/AI-website/issues/7> |
| T013 | #8 | <https://github.com/nn54ym5b2d-dot/AI-website/issues/8> |
| T014 | #9 | <https://github.com/nn54ym5b2d-dot/AI-website/issues/9> |
| T015 | #10 | <https://github.com/nn54ym5b2d-dot/AI-website/issues/10> |
| T016 | #11 | <https://github.com/nn54ym5b2d-dot/AI-website/issues/11> |
| T017 | #16 | <https://github.com/nn54ym5b2d-dot/AI-website/issues/16> |

每个任务默认规则：

- 每次只执行一个 Issue。
- 执行前必须读取 `PROJECT_START.md`、`AGENTS.md`、`docs/项目总控.md` 和相关 docs。
- 如任务中发现文档与需求冲突，以 `docs/决策记录.md` 和 Robert 最新明确指令为准。
- 完成后需要运行必要检查，并同步更新 Notion 总控页、`docs/项目总控.md`、`docs/任务清单.md` 和对应 Issue。

---

## [T006] 设计第一版页面结构和导航

任务目标：
明确第一版 Web 网站、上传者中心、个人中心、管理后台和外部观察员后台的页面结构、路由、导航关系和入口优先级。

相关资料：
- `docs/PRD.md`
- `docs/页面清单.md`
- `docs/角色权限.md`
- `docs/业务规则.md`
- `app/`
- `components/layout/site-header.tsx`

关联任务编号：
T006

本次范围：
- 梳理第一版页面分组、路由命名、导航层级和角色入口。
- 明确前台、上传者、购买用户、管理员、外部观察员分别从哪里进入。
- 更新 `docs/页面清单.md`，必要时同步 `docs/项目总控.md`。
- 在代码中建立结构优先的页面骨架和导航雏形，重点是路由和信息结构，不追求最终视觉精修。

不做范围：
- 不做最终品牌视觉设计。
- 不开发真实登录、支付、上传、订单、下载功能。
- 不接入数据库、COS、支付或认证外部服务。

影响范围：
- `docs/页面清单.md`
- `docs/项目总控.md`
- `app/`
- `components/`

验收标准：
- Robert 能看懂第一版网站有哪些页面、每类用户从哪里进入。
- 页面结构覆盖前台浏览、上传者、购买用户、管理后台、外部观察员只读看板。
- T007、T008 能基于页面结构继续细化数据库和 API。
- 当前页面骨架在桌面和手机宽度下不出现明显布局错乱。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 如有代码改动，运行 `npm run build`

备注：
T006 是结构和导航优先，不是最终 UI 定稿。

---

## [T007] 设计数据库表结构细化版

任务目标：
把当前数据库草案细化为第一版可开发的数据模型，明确核心表、字段、关系、状态枚举、索引和权限敏感字段。

相关资料：
- `docs/数据库设计.md`
- `docs/业务规则.md`
- `docs/角色权限.md`
- `docs/API.md`
- `docs/页面清单.md`

关联任务编号：
T007

本次范围：
- 细化用户、登录身份、上传者、邀请码、素材、素材文件、认证、订单、支付、退款、授权、下载、收益、后台角色、操作日志、外部观察员汇总等数据结构。
- 明确文件存储只保存 COS key/path，不保存公开原始文件地址。
- 明确人物证明材料和认证证书文件的保存字段。
- 明确外部观察员可见汇总数据和不可见敏感数据。
- 输出数据库实现建议，包括是否需要 Prisma、Drizzle 或 SQL migration 方案。

不做范围：
- 不连接真实数据库。
- 不创建生产数据表。
- 不写完整业务接口。

影响范围：
- `docs/数据库设计.md`
- `docs/技术方案.md`
- `docs/API.md`
- 后续数据库实现任务

验收标准：
- 每个核心业务流程都能在数据模型中找到对应记录。
- 授权、下载、收益、认证证书和外部观察员数据不缺关键字段。
- 明确哪些字段需要脱敏、哪些字段不能给外部观察员看。
- 明确后续实现数据库迁移时的工具建议和待确认点。

测试要求：
- 文档任务可不运行代码检查。
- 若改动类型文件或代码占位，运行 `npm run typecheck`。

备注：
如果需要新增技术选择，比如 ORM，请列出建议并等待 Robert 确认。

---

## [T008] 设计核心 API 细化版

任务目标：
把 API 草案细化为第一版可开发的接口说明，明确接口路径、方法、权限、请求参数、返回结构、错误状态和安全要求。

相关资料：
- `docs/API.md`
- `docs/数据库设计.md`
- `docs/业务规则.md`
- `docs/角色权限.md`
- `docs/测试验收清单.md`

关联任务编号：
T008

本次范围：
- 细化用户登录、邀请码激活、素材浏览、素材上传、审核认证、订单支付、授权下载、收益记录、后台管理、外部观察员只读接口。
- 明确每个接口允许哪些角色访问。
- 明确支付回调、COS 限时下载、文件上传方式的接口边界。
- 明确通用错误码、鉴权失败、权限不足、数据不存在、状态不允许等返回规则。
- 更新 `docs/API.md`，必要时同步 `docs/测试验收清单.md`。

不做范围：
- 不实现全部 API。
- 不接入真实微信支付、支付宝、COS 或认证网站。
- 不输出正式 OpenAPI 文件，除非 Robert 另行确认。

影响范围：
- `docs/API.md`
- `docs/测试验收清单.md`
- 后续前后端开发任务

验收标准：
- 后续 T009-T015 能按接口说明开发。
- 每个核心流程至少有清晰的请求、返回、权限和错误处理说明。
- 外部观察员接口明确只返回汇总或脱敏字段，不提供导出接口。

测试要求：
- 文档任务可不运行代码检查。
- 如新增或修改代码，运行 `npm run lint`、`npm run typecheck`、`npm test` 和 `npm run build`。

备注：
API 细化完成后，再决定是否需要 Apifox。

---

## [T009] 开发用户注册登录和角色基础

任务目标：
建立第一版用户身份和角色基础，让系统具备购买用户、上传者、管理员、财务、外部观察员等角色判断能力。

相关资料：
- `docs/角色权限.md`
- `docs/API.md`
- `docs/数据库设计.md`
- `lib/auth/roles.ts`
- `types/domain.ts`

关联任务编号：
T009

本次范围：
- 实现基础角色类型、权限判断和页面访问守卫雏形。
- 使用 Docker Compose + PostgreSQL 16 + Prisma，建立 schema、首个 migration、可重复初始化的本地数据库和最小测试数据。
- 实现注册、登录、退出、当前用户信息、邀请码激活上传者身份的可运行本地 API；手机号、邮箱、微信由 provider adapter 隔离。
- 保存手机号/邮箱注册和微信首次注册接受的有效条款版本。
- 支持手机号、邮箱、微信登录的结构设计，但真实短信、邮箱验证码、微信 OAuth 可先占位。
- 给上传者、管理员、外部观察员准备不同入口和权限状态。

不做范围：
- 不接入真实短信服务。
- 不接入真实微信登录。
- 不实现复杂账号安全策略。
- 不做最终 UI 视觉精修。

影响范围：
- `app/`
- `lib/auth/`
- `types/`
- `docs/API.md`
- `docs/测试验收清单.md`

验收标准：
- 代码中能清楚区分购买用户、上传者、超级管理员、运营、财务、外部观察员。
- 页面或接口能根据角色显示/限制基础入口。
- 外部观察员不能获得编辑、审核、退款、导出等权限。
- 全新环境可按文档启动数据库、执行 migration 并跑通身份 API 自动化测试。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm test`
- 运行 `npm run build`

备注：
本任务先做身份和权限基础，不做真实第三方登录。

---

## [T009A] 源素库 MVP 视觉设计基线与核心页面原型

任务目标：
在 T010-T016 功能开发前，建立经 Robert 确认、可在后续页面复用的源素库 MVP 视觉设计基线，并完成核心页面的响应式可运行原型。

相关资料：
- `PROJECT_START.md`
- `docs/项目总控.md`
- `docs/PRD.md`
- `docs/页面清单.md`
- `docs/角色权限.md`
- `docs/视觉设计基线.md`
- `docs/测试验收清单.md`
- `docs/决策记录.md`

关联任务编号：
- 前置：T006、T009
- 当前：T009A
- 后续：T010-T016

本次范围：
- 整理参考网站、截图或品牌关键词，形成最小设计简报；没有参考时使用已确认的默认设计简报。
- 使用相同页面内容生成三套差异明确的视觉方向，不先写正式页面代码。
- 由 Robert 选择一套主方向，可对选中方向提出有限调整。
- 将选中方向固化为颜色、字体、字号、间距、圆角、边框、阴影、图片比例、图标和组件状态规则。
- 完成首页、素材浏览/搜索、素材详情、登录/注册、个人中心/上传、管理后台六组代表页面的响应式可运行原型。
- 至少覆盖约 1440px 桌面端和约 390px 手机端。
- 核心导航、搜索筛选、标签切换、登录注册表单和主要按钮可操作；使用明确标记的非真实演示素材与数据。
- 形成可供 T010-T016 复用的公共 UI 组件和 `docs/视觉设计基线.md`。

不做范围：
- 不接入素材数据库、真实素材查询 API、支付、COS、上传、审核、下载或认证服务。
- 不改变 T006 已确认的路由、角色入口和信息结构。
- 不改变 T009 的身份、Cookie、CSRF、邀请码和角色权限逻辑。
- 不完成所有后台子页面，不制作完整企业品牌手册，不要求本任务确定最终 Logo。
- 不让三套视觉方向同时进入开发；必须先选定一套主方向。
- 不把演示数据或生成素材描述为真实业务数据或已接通外部服务。

影响范围：
- `app/globals.css`
- `tailwind.config.ts`
- `components/layout/`
- `components/navigation/`
- 首页、素材浏览/搜索、素材详情、登录/注册、个人中心/上传、管理后台代表页面
- `docs/视觉设计基线.md` 和相关项目状态资料

验收标准：
- Robert 已从三套方案中选择一套明确的主视觉方向。
- 视觉基线文档记录颜色、字体、间距、图片比例和组件规范。
- 六组代表页面使用统一视觉，桌面端与手机端均可正常浏览。
- 素材图片是视觉重点，认证状态、价格和主要操作层级清楚。
- 核心导航和基础交互可操作，演示素材与数据明确标记为非真实。
- 登录注册和既有角色权限没有被视觉改动破坏。
- 未提前接入 T010-T017 的真实业务功能或外部服务。
- T010 能直接复用本任务确认的视觉规则和公共组件。

测试要求：
- 同视口对比选中视觉稿与页面实现，检查布局、裁切、留白、字体、颜色、边框和圆角。
- 检查桌面端与手机端，不允许横向溢出或关键操作被遮挡。
- 检查键盘焦点、颜色对比度、表单标签、按钮可点击区域和常见状态。
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm test`
- 运行 `npm run build`
- GitHub Actions `verify` 通过。

备注：
- GitHub Issue #21，已加入 Project #1；本地状态为 Ready，Project 状态为 Todo；项目资料同步见 PR #22。
- 当前默认简报：专业、可信、克制、具有创意、突出素材图片。
- 当前先建立任务和同步资料，尚未生成三套方向或修改正式页面。
- T009A 验收完成后，T010 才从 Blocked 调整为 Ready。

---

## [T010] 开发素材浏览和详情页

任务目标：
开发第一版素材浏览、分类、搜索筛选和素材详情页骨架，让购买用户能理解素材列表和购买入口。

相关资料：
- `docs/页面清单.md`
- `docs/业务规则.md`
- `docs/API.md`
- `docs/数据库设计.md`

关联任务编号：
T010

本次范围：
- 开发首页素材入口、分类页、搜索结果页、素材详情页。
- 支持人物、物件/道具、场景素材的基础展示。
- 展示认证状态、价格、预览信息和购买入口。
- 基于 T009 的本地 PostgreSQL/Prisma 数据层实现素材查询 API；界面可保留少量演示种子数据，但不能绕过 API 直读 mock。

不做范围：
- 不做真实支付。
- 不开放原始文件下载。
- 不接入真实搜索服务。
- 不做最终商业视觉定稿。

影响范围：
- `app/`
- `components/`
- `lib/domain/`
- `types/`

验收标准：
- 用户能浏览素材列表、按类型进入分类、查看素材详情。
- 素材详情页明确显示价格、认证状态和购买入口。
- 未登录或未购买用户不能看到原始下载链接。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm test`
- 运行 `npm run build`

备注：
本任务以可用浏览和本地真实数据闭环为主；腾讯云生产数据接入后续处理。

---

## [T011] 开发上传者素材提交流程

任务目标：
开发上传者提交人物、物件/道具、场景素材的第一版流程，包括素材基础信息、文件信息、证明材料和提交审核状态。

相关资料：
- `docs/业务规则.md`
- `docs/页面清单.md`
- `docs/API.md`
- `docs/数据库设计.md`
- `lib/storage/tencent-cos.ts`

关联任务编号：
T011

本次范围：
- 开发上传者素材创建/编辑/提交页面。
- 区分人物、物件/道具、场景素材表单规则。
- 人物素材要求必要证明材料；物件/场景不强制证明材料。
- 所有素材必须进入认证流程，获得认证证书后才能上架。
- 建立文件上传 provider adapter、受控本地测试实现、上传意图和文件元数据持久化；仅人物素材要求证明材料。

不做范围：
- 不接入真实 COS 上传，除非 Robert 已准备好 Bucket 和密钥。
- 不处理真实认证上传费支付。
- 不实现真实政府认证跳转。

影响范围：
- `app/`
- `components/`
- `lib/storage/`
- `types/`
- `docs/API.md`
- `docs/数据库设计.md`

验收标准：
- 上传者能看到三类素材的提交入口。
- 人物素材证明材料规则被明确执行或提示。
- 素材提交后进入待审核/认证相关状态。
- 原文件地址不被公开展示。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm test`
- 运行 `npm run build`

备注：
腾讯云 COS 未接入前使用受控本地测试 provider 跑通 API，不能把测试地址描述成真实 COS。

---

## [T012] 开发后台素材审核和认证记录

任务目标：
开发管理后台的素材审核、认证证书记录和上架控制基础能力。

相关资料：
- `docs/业务规则.md`
- `docs/角色权限.md`
- `docs/页面清单.md`
- `docs/API.md`
- `docs/数据库设计.md`

关联任务编号：
T012

本次范围：
- 开发后台素材审核列表和详情。
- 实现审核通过、审核驳回、上架、下架的服务端状态流转和数据库约束。
- 支持录入认证状态、证书编号、凭证和认证证书文件记录。
- 所有审核、认证和上架状态通过本地 API 与数据库事务持久化，不只改变前端状态。
- 明确只有认证完成后素材才可上架。
- 记录关键后台操作日志的结构或占位。

不做范围：
- 不自动对接政府认证网站。
- 不做真实证书 OCR 或自动校验。
- 不允许外部观察员操作审核或认证。

影响范围：
- `app/admin/`
- `lib/auth/`
- `types/`
- `docs/API.md`
- `docs/测试验收清单.md`

验收标准：
- 运营/超级管理员能看到审核和认证入口。
- 财务管理员和外部观察员不能审核、上架、录入认证。
- 认证证书文件和证书编号字段在流程中有明确位置。
- 未认证素材不能进入已上架状态。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm test`
- 运行 `npm run build`

备注：
政府认证网站和证书样式仍是待确认项，先按人工录入设计。

---

## [T013] 开发订单、支付测试流程和授权记录

任务目标：
开发多素材订单、可运行的本地支付测试流程和支付成功后生成授权记录的基础能力。

相关资料：
- `docs/业务规则.md`
- `docs/API.md`
- `docs/数据库设计.md`
- `docs/测试验收清单.md`

关联任务编号：
T013

本次范围：
- 建立订单创建、订单明细、支付状态、退款记录的前后端结构。
- 支持一个订单包含多个素材。
- 支付成功后为每个素材生成独立授权记录。
- 支付方式结构支持微信支付和支付宝；本任务使用可验证的本地测试 provider，真实商户接口在 T017 接入。
- 实现可验证签名、金额校验和重复事件去重的本地测试 webhook provider；素材购买退款按完整订单明细记录 `refund_items`。
- 更新相关文档中的支付和授权边界。

不做范围：
- 不接入真实微信支付/支付宝商户接口。
- 不使用真实商户密钥或调用生产支付网关；但必须用测试密钥/固定夹具验证回调校验与幂等逻辑。
- 不实现复杂分账或自动提现。

影响范围：
- `app/`
- `lib/`
- `types/`
- `docs/API.md`
- `docs/数据库设计.md`

验收标准：
- 用户能从素材详情或订单入口进入本地支付测试流程，页面明确标识测试 provider。
- 一个订单可包含多个素材。
- 经验证的测试支付成功回调后，能幂等生成授权、ZIP 下载资格和初始收益记录。
- 退款记录结构不破坏收益和授权逻辑。
- 重复支付回调不重复生成授权、ZIP 下载资格或收益，拆分单个订单明细金额的退款被拒绝。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm test`
- 运行 `npm run build`

备注：
真实支付接入前，必须保持密钥不进入代码仓库。

---

## [T014] 开发 ZIP 下载和收益记录

任务目标：
开发购买后限时下载、下载记录和上传者收益记录的基础能力。

相关资料：
- `docs/业务规则.md`
- `docs/API.md`
- `docs/数据库设计.md`
- `docs/测试验收清单.md`
- `lib/storage/tencent-cos.ts`

关联任务编号：
T014

本次范围：
- 建立已购素材下载入口和下载记录。
- 实现下载资格校验、ZIP 就绪状态和短时签名地址的本地测试服务。
- 将每份素材的全部有效原文件按固定清单打包为一个 ZIP，并通过本地测试存储 provider 验证下载权限。
- 实现上传者收益记录和收益汇总页面/API，并从本地数据库读取。
- 明确原始文件地址不能直接公开。
- 下载入口默认有效 365 天，并以配置项保留后续调整能力；每次实际下载再生成短时 COS 签名 URL。

不做范围：
- 不公开 COS 原始文件地址。
- 不做真实自动提现。
- 不做复杂结算系统。

影响范围：
- `app/`
- `lib/storage/`
- `types/`
- `docs/API.md`
- `docs/数据库设计.md`

验收标准：
- 已购用户能看到可下载素材入口。
- 未购买用户不能获取下载链接。
- 下载动作有记录。
- ZIP 只包含所属素材的全部有效原文件，入口可在 365 天内重复使用，退款/授权撤销后立即失效。
- 上传者收益记录能基于订单明细产生。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm test`
- 运行 `npm run build`

备注：
下载入口有效期已由 Robert 确认为默认 365 天。

---

## [T015] 开发后台权限和外部观察员只读视图

任务目标：
完善后台权限控制，并开发外部观察员只读数据看板，确保合作方只能查看允许范围内的平台汇总和分成相关数据。

相关资料：
- `docs/角色权限.md`
- `docs/业务规则.md`
- `docs/API.md`
- `docs/数据库设计.md`
- `lib/auth/roles.ts`

关联任务编号：
T015

本次范围：
- 完善超级管理员、运营、财务、外部观察员的后台权限判断。
- 开发外部观察员只读数据看板。
- 实现 `/api/v1/observer/*` 只读汇总 API，并从平台周期快照及分类型快照读取。
- 展示平台上传量、下载量、订单金额、净收益、合作方分成基数、比例和金额；首版观察员分成比例和金额为 0。
- 隐藏用户手机号、邮箱、证件信息、支付账户、完整支付流水号、原文件地址、下载原始链接和内部操作日志。
- 禁止外部观察员导出数据。

不做范围：
- 不允许外部观察员编辑、审核、退款、录入认证、修改价格或管理账号。
- 不开放观察员写接口、导出接口或业务明细 API。
- 不做真实结算付款。

影响范围：
- `app/admin/`
- `lib/auth/`
- `types/`
- `docs/API.md`
- `docs/测试验收清单.md`

验收标准：
- 外部观察员只能访问只读看板。
- 外部观察员看不到敏感字段和内部日志。
- 外部观察员没有导出入口。
- 观察员 API 可返回人物、物件/道具、场景分类型汇总，但不能下钻到单个素材、订单、用户或下载记录。
- 管理员、运营、财务权限边界符合 `docs/角色权限.md`。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm test`
- 运行 `npm run build`

备注：
这是权限敏感任务，完成后需要重点 review。

---

## [T016] MVP 全流程测试和修正

任务目标：
对第一版 MVP 进行端到端检查，验证上传、审核、认证记录、购买、授权、下载、收益、后台权限和外部观察员只读视图是否能串起来。

相关资料：
- `docs/测试验收清单.md`
- `docs/PRD.md`
- `docs/业务规则.md`
- `docs/角色权限.md`
- `docs/API.md`
- `docs/数据库设计.md`

关联任务编号：
T016

本次范围：
- 按购买用户、上传者、超级管理员、运营、财务、外部观察员分别走核心流程。
- 修正阻塞 MVP 验收的明显问题。
- 检查敏感信息、权限边界、下载链接、授权记录、收益记录是否符合规则。
- 更新验收结果和剩余风险。

不做范围：
- 不加入新业务大功能。
- 不做 App、小程序、会员、自动提现、复杂分账。
- 不把待确认的法律协议、政府认证网站、真实支付接口强行假定为已完成。

影响范围：
- 全项目
- `docs/测试验收清单.md`
- `docs/项目总控.md`
- `docs/任务清单.md`

验收标准：
- 本地 MVP 核心流程能用真实数据库、可替换测试 provider 和自动化测试完整跑通；只有外部服务调用可明确标记为待 T017 接入。
- 所有 P0 权限边界通过检查。
- GitHub Actions 通过。
- Notion、项目总控、任务清单和 GitHub Issue 状态同步。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm test`
- 运行 `npm run build`
- 补充必要的手动验收记录。

备注：
本任务完成后，项目进入上线准备和真实外部服务接入检查阶段。

---

## [T017] 真实外部服务、腾讯云部署与上线准备

任务目标：
在本地 MVP 通过 T016 后，接入获批的真实外部服务并完成中国大陆生产部署前检查，不把开发占位直接带入生产。

相关资料：
- `docs/技术方案.md`
- `docs/API.md`
- `docs/数据库设计.md`
- `docs/测试验收清单.md`

关联任务编号：
T017

本次范围：
- 接入已开通的腾讯云 COS 与 TencentDB for PostgreSQL，并验证最小权限、备份和恢复流程。
- 按已提供的商户与平台配置接入微信支付、支付宝、短信/邮件和微信登录；验证真实回调、重试和对账。
- 完成腾讯云应用部署、HTTPS、域名、备案、环境变量和监控告警检查。
- 执行生产前安全、隐私、数据保留、密钥轮换和回滚演练。

不做范围：
- 不新增会员、企业授权、自动提现、App 或小程序等新业务。
- 不在缺少账号、授权、正式协议或备案条件时伪造上线完成。

验收标准：
- 所有外部服务状态有真实证据和失败回退方案。
- 生产密钥不进入仓库，环境权限遵循最小授权。
- 关键交易、退款、ZIP 下载、备份恢复和部署回滚完成生产前演练。

测试要求：
- 运行 `npm run lint`
- 运行 `npm run typecheck`
- 运行 `npm test`
- 运行 `npm run build`
- 完成单独的生产前手动验收记录。
